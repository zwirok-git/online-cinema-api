async def send_activation_email(email: str, token: str) -> None:
    # SMTP or Celery integration can be added here later.
    return None


async def send_password_reset_email(email: str, token: str) -> None:
    # SMTP or Celery integration can be added here later.
    return None
